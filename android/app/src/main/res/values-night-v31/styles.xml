<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tema escuro do sistema: mantém fundo branco igual ao light e à splash
         do Capacitor. Fundo #111111 deixava a abertura preta com o ícone
         pequeno no centro — visual ruim. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#FFFFFF</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">#FFFFFF</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>
