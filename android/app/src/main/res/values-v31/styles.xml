<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Android 12+: ícone da marca no círculo + fundo na cor clara da identidade.
         Em seguida o Capacitor SplashScreen mostra a splash completa
         (logo + AlugaPro + tagline) via @drawable/splash. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#E8F0FA</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="windowSplashScreenIconBackgroundColor">#E8F0FA</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>
